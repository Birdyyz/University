alunos_aprovacao <- read.csv("~/GitHub/Universidade/3ºano/AE/Aula8/alunos_aprovacao.csv")
View(alunos_aprovacao)
knitr::opts_chunk$set(echo = TRUE)
library(ggplot2)
dados <- alunos_aprovacao
summary(dados)
# tabela de frequencias
table(dados$aprovado)
# tabela de frequencias
table(dados$aprovado)
table(dados$trabalhador)
table(dados$aprovado)
table(dados$trabalhador)
table(dados$aprovado, dados$trabalhador)
# estatisticas resumo
summary(dados$horas_estudo,dados$frequencia_aulas)
# estatisticas resumo
summary(dados$horas_estudo,dados$frequencia_aulas,dados$media_secundario)
library(ggplot2)
ggplot(dados, aes(x = horas_estudo, y = aprovado)) +
geom_boxplot(fill = "gray")
ggplot(dados, aes(x = aprovado, y = horas_estudo)) +
geom_boxplot(fill = "gray")
ggplot(dados, aes(x = aprovado, y = horas_estudo)) +
geom_line()
dados <- alunos_aprovacao
summary(dados)
# tabela de frequencias
table(dados$aprovado)
table(dados$trabalhador)
table(dados$aprovado, dados$trabalhador)
# estatisticas resumo
summary(dados$horas_estudo,dados$frequencia_aulas,dados$media_secundario)
library(ggplot2)
ggplot(dados, aes(x = aprovado, y = horas_estudo)) +
geom_boxplot(fill = "gray")
ggplot(dados, aes(x = aprovado, y = horas_estudo)) +
geom_line()
modeloap <- glm(aprovado~horas_estudo + frequencia_aulas + trabalhador, data = dados)
View(modeloap)
summary(modeloap)
modeloap <- glm(aprovado~horas_estudo + frequencia_aulas + trabalhador, data = dados,family = binomial(link = "logit"))
summary(modeloap)
exp(coef(modeloap))["trabalhador"]
modelomc <- glm(glm(aprovado ~ horas_estudo + frquencia_aulas + trabalhador + media_secundari , data = dados,family = binomial(link = "logit")))
modelomc <- glm(glm(aprovado ~ horas_estudo + frequencia_aulas + trabalhador + media_secundari , data = dados,family = binomial(link = "logit")))
modelomc <- glm(glm(aprovado ~ horas_estudo + frequencia_aulas + trabalhador + media_secundario , data = dados,family = binomial(link = "logit")))
summary(modelomc)
predict(modelomc, newdata = data.frame(horas_estudo  = 2, frequencia_aulas =60, trabalhador  = 1, media_secundario = 12),type="response")
predict(modelomc, newdata = data.frame(horas_estudo  = 8, frequencia_aulas =90, trabalhador  = 0, media_secundario = 12),type="response")
predict(modelomc, newdata = data.frame(horas_estudo  = 5, frequencia_aulas =80, trabalhador  = 0, media_secundario = 17),type="response")
modelomc <- glm(glm(aprovado ~ horas_estudo + frequencia_aulas + trabalhador + media_secundario , data = dados,family = binomial(link = "logit")))
modelomc <- glm(glm(aprovado ~ horas_estudo + frequencia_aulas + trabalhador + media_secundario , data = dados,family = binomial(link = "logit")))
summary(modelomc)
predict(modelomc, newdata = data.frame(horas_estudo  = 2, frequencia_aulas =60, trabalhador  = 1, media_secundario = 12),type="response")
predict(modelomc, newdata = data.frame(horas_estudo  = 2, frequencia_aulas =60, trabalhador  = 1, media_secundario = 12),type="response")
predict(modelomc, newdata = data.frame(horas_estudo  = 8, frequencia_aulas =90, trabalhador  = 0, media_secundario = 12),type="response")
predict(modelomc, newdata = data.frame(horas_estudo  = 5, frequencia_aulas =80, trabalhador  = 0, media_secundario = 17),type="response")
predict(modelomc,
newdata = data.frame(
horas_estudo = 2,
frequencia_aulas = 60,
trabalhador = 1,
media_secundario = 12
),
type = "response")
predict(modelomc, newdata = data.frame(horas_estudo  = 8, frequencia_aulas =90, trabalhador  = 0, media_secundario = 12),type="response")
predict(modelomc, newdata = data.frame(horas_estudo  = 5, frequencia_aulas =80, trabalhador  = 0, media_secundario = 17),type="response")
modelomc <- glm(aprovado ~ horas_estudo + frequencia_aulas + trabalhador + media_secundario , data = dados,family = binomial(link = "logit"))
summary(modelomc)
predict(modelomc, newdata = data.frame(horas_estudo  = 2, frequencia_aulas =60, trabalhador  = 1, media_secundario = 12),type="response")
predict(modelomc, newdata = data.frame(horas_estudo  = 8, frequencia_aulas =90, trabalhador  = 0, media_secundario = 12),type="response")
predict(modelomc, newdata = data.frame(horas_estudo  = 5, frequencia_aulas =80, trabalhador  = 0, media_secundario = 17),type="response")
dados$am_pred <- ifelse(mtcars$prob_manual >= 0.5)
dados$am_pred <- ifelse(dados$prob_manual >= 0.5)
dados$prob_aprovacao <- predict(modelomc, type = "response")
dados$aprov_pred <- ifelse(dados$prob_aprovacao >= 0.5, 1, 0)
table(Real = dados$aprovado, Predito = dados$aprov_pred)
dados$prob_aprovacao <- predict(modelomc, type = "response")
dados$aprov_pred <- ifelse(dados$prob_aprovacao >= 0.5, 1, 0)
table(Real = dados$aprovado, Predito = dados$aprov_pred)
# Soma dos acertos / total
mean(dados$aprovado == dados$aprov_pred)
```{r exercicio2-avaliacao}
dados$prob_aprovacao <- predict(modelomc, type = "response")
dados$aprov_pred <- ifelse(dados$prob_aprovacao >= 0.5, 1, 0)
cm <- table(Real = dados$aprovado, Predito = dados$aprov_pred)
cm
TP <- cm["1","1"]
FN <- cm["1","0"]
FP <- cm["0","1"]
TN <- cm["0","0"]
sensibilidade <- TP / (TP + FN)
especificidade <- TN / (TN + FP)
sensibilidade
especificidade
dados$prob_aprovacao <- predict(modelomc, type = "response")
dados$aprov_pred <- ifelse(dados$prob_aprovacao >= 0.5, 1, 0)
cm <- table(Real = dados$aprovado, Predito = dados$aprov_pred)
cm
TP <- cm["1","1"]
FN <- cm["1","0"]
FP <- cm["0","1"]
TN <- cm["0","0"]
sensibilidade <- TP / (TP + FN)
especificidade <- TN / (TN + FP)
sensibilidade
especificidade
